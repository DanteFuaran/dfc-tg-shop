#!/bin/bash
# Скрипт для настройки git hooks

HOOKS_DIR=".githooks"
GIT_HOOKS_DIR=".git/hooks"

echo "🔧 Настройка git hooks..."

# Проверяем наличие папки .githooks
if [ ! -d "$HOOKS_DIR" ]; then
    echo "❌ Папка $HOOKS_DIR не найдена"
    exit 1
fi

# Настраиваем путь к hooks
git config core.hooksPath "$HOOKS_DIR"

# Делаем hooks исполняемыми
chmod +x "$HOOKS_DIR"/*

echo "✅ Git hooks настроены!"
echo "   Теперь при клонировании репозитория установка будет запускаться автоматически"
